The following SQL queries are exercises that:
  - Display the first 100 rows
  - Filter for rows where there were 0 delays
  - Count the number of deliveries with a weight > 5kg
  - Summarise the delays by city
